// #define RAY_TRACING#
#include "v4d/modules/V4D_raytracing/glsl_includes/pl_raycast.glsl"

#shader comp

void main() {
	// vec2 imageSize = vec2(imageSize(img_gBuffer_1));
	// ivec2 imageOffset = ivec2(round(imageSize / 2));
	// buffer_normal_uv = vec4(imageLoad(img_gBuffer_1, imageOffset));
	// buffer_position_distance = vec4(imageLoad(img_gBuffer_2, imageOffset));
	// buffer_custom = uvec4(imageLoad(img_gBuffer_4, imageOffset));
}
